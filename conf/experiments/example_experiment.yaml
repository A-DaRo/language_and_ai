# conf/experiments/example_experiment.yaml - Example experiment configuration

defaults:
  - /base/pipeline

experiment_name: "example_pollution_mitigation"

gliner:
  confidence_threshold: 0.90

encoder:
  model: "roberta-large"

training:
  num_epochs: 5

paths:
  output: "./artifacts/experiments/example_experiment"
