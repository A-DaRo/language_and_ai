# conf/laptop/training.yaml - Laptop training configuration

defaults:
  - /base/training

training:
  num_epochs: 2
  gradient_accumulation_steps: 4

optimizer:
  lr: 2e-5

dataloader:
  batch_size: 8

precision:
  dtype: "float16"
  use_grad_scaler: true
