# Test-only Phase A config fragment.
# Merged on top of base + mode pipeline configs.

subset:
  enabled: true
  size: 12

gliner:
  batch_size: 2
  compute_explicit_recall: false

encoder:
  batch_size: 2

probe:
  compute_amnesic_drop: false

quality:
  enforce_thresholds: false
